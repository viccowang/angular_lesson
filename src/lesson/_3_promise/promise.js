import angular from 'angular';
import $ from 'jquery';

/**
 * Ajax & Promise
 */
const promiseModule = angular.module('promiseModule',[]);

promiseModule.controller('promiseCtrl', ['$scope', ($scope) => {


    
}]);


export default promiseModule;